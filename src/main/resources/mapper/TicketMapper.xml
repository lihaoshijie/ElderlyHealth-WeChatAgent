<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.elderlyhealth.agent.mapper.TicketMapper">
    <insert id="insert">
        INSERT INTO tickets(ticket_no, order_no, user_id, attraction_name, visit_date, quantity, unit_price, total_price, status, qr_data)
        VALUES(#{ticketNo}, #{orderNo}, #{userId}, #{attractionName}, #{visitDate}, #{quantity}, #{unitPrice}, #{totalPrice}, #{status}, #{qrData})
    </insert>
</mapper>
